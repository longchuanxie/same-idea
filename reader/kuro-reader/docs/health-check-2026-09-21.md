# kuro-reader 三维度体检报告（2026-09-21，第四轮）

> 基线：`comics` @ `3abad9d`（听书起播分页等待+跨章预热收尾提交，其上 918 例全绿）。实施分支：`optimize-20260921`（自基线切出）。
> 前三轮体检见 `docs/health-check-2026-09-17.md` 与 `docs/health-check-2026-09-19.md`；本轮编号从 UX-001/E-001 重新起算。
> 证据约定：`文件:行号` 指基线/实施时点；浏览器动态实测项标注「实测」。

---

## 阶段〇：基线快照与特别事项

| 检查项 | 结果 | 证据 |
|---|---|---|
| 自动化测试 | 918/918 全绿（112 文件） | `npm test` @ 3abad9d |
| ESLint + 令牌守卫 / TypeScript | 0 错 | `npm run lint` / `tsc --noEmit` |
| 工作区 | 基线前将未提交的听书分页等待工作独立成 3abad9d 提交，建立干净基线 | git |

**特别事项——会话内出现并行开发流**：实施中途（08:17 起）工作区出现另一条工作流的未提交改动（`src/plugins/TextFocusPlugin.ts`、`src/services/tts/ttsNotification.ts`、`@capacitor/local-notifications` 依赖、TextReader 内的通知栏接线等，均为听书通知栏新功能）。处置：
1. 全程未 staged / 未提交任何外部文件——本报告所列 commit 均为定向 `git add` 或「仅暂存本 hunk」的部分补丁（`git apply --cached`），外部改动原样留在工作区；
2. 期间外部代码短暂引入 4 个 TS 错误（ttsNotification 系列），本轮自测改用「错误归属过滤」（排除外部文件后 0 错）；至回归阶段外部工作流已自行修复，全量 tsc 复归 0 错；
3. vite 监视器在高频并行文件变更下漏事件（提供旧代码），重启 dev server 后恢复——回归阶段结论以重启后的服务为准。

---

## 阶段一：用户需求匹配度分析

### 1.1 功能清单核验

上轮 F1–F48 清单（`docs/health-check-2026-09-17.md` §1.1）经全路由冒烟（14 路由，实测）与模块级核验**全部仍成立**；上轮全部修复项（立即开读/侧栏导入/autoFocus/Ctrl+A/settings 锚点/检索台标题等）在位。基线后新增能力：听书起播分页等待与跨章预热（3abad9d）、听书通知栏（并行工作流，未入库）。

### 1.2 需求匹配度矩阵（按模块汇总，仅列有缺口项）

| 模块 | 轻度 | 重度 | 缺口与证据 → 本轮处置 |
|---|---|---|---|
| 特藏室批量管理（F27） | 部分满足 | **部分满足** | 批量条被移动底栏盖住约 39px（`SubLibrary:330` 无避让 margin，实测 y743+77 > 底栏 y781）→ **UX-001**；多选即删无确认（`SubLibrary:86-96`）→ **UX-002**；标已读不落库（`useLibraryStore:864`）→ **E-003** |
| 书架筛选（F25） | 部分满足 | 满足 | 筛选空态误指「去导入/已归特藏室」且无一键清除（`Library:852-864`，实测确认）→ **UX-003** |
| 听书（F14） | 满足 | 部分满足 | 引擎失败后控制条假活（`TextReader:993` 仅 toast）→ **UX-004**；起播等待窗口暂停无效（`useSpeech:326` 早退 × `TextReader` 轮询，实测）→ **E-005** |
| AI 类功能（F33-37/41） | 部分满足 | 满足 | 回回想未配置出路只在 5 秒 toast（`ReadingRecapSheet:88-93`）→ **UX-005**；问藏书纯漫画馆藏误导报错（`Ask:55-59` 无空态分支）→ **UX-007** |
| 原生导入（F24） | 部分满足 | 满足 | 选文件读取失败静默（`Import:233-245/299-301` catch 吞掉）→ **UX-006** |
| 数据可靠性（F44/45） | 满足 | **部分满足** | 多标签页 DB 升级挂起零反馈（`db.ts` 三回调全缺）→ **E-001**；OPDS 中文凭据静默降级（`opds.ts:66-74`）→ **E-002**；恢复备份进度无条件清库（`Settings:279`）→ **E-004** |
| 桌面效率 | 满足 | 部分满足 | 无全局检索快捷键 → **UX-010**（继承 U-001） |
| 其余模块（阅读核心/图页/馆藏其余/学习回顾/系统） | 满足 | 满足 | 上轮判定维持；新代码静态审查仅 4 个 P3 边角（见 3.1） |

### 1.3 洞察清单

**轻度用户上手障碍 TOP3**
1. 纯漫画馆藏进「问藏书」得到伪错误反馈（UX-007）——首次使用即被误导。
2. 回想想 AI 未配置只有 5 秒 toast 出路（UX-005）——错过即「点了没反应」。
3. 原生端选完文件毫无反应（UX-006）——首书导入的关键转化点断点。

**重度用户效率瓶颈 TOP3**
1. 特藏室批量三键被底栏遮挡误触（UX-001，P1，实测确认）——文件夹导入漫画的主阵地。
2. 批量标已读重启即丢（E-003）+ 批量删除无确认（UX-002）——批量劳动成果不可信。
3. 数据静默损坏三类：多标签页升级挂起（E-001）、OPDS 中文凭据连不上且报错误导（E-002）、恢复备份清空进度（E-004）。

---

## 阶段二：交互与用户动线（方案 → 实施见阶段四）

### 2.1 核心动线现状与实施后对比（均为实测）

| 动线 | 实施前 | 实施后 |
|---|---|---|
| a) 特藏室批量管理（手机） | 三键下半部被底栏盖住，误触高发 | 批量条抬升 80px，三键完整可点（实测 y663+77 < 底栏 y781） |
| b) 特藏室批量删除 | 多选即删，无确认无撤回 | ConfirmDialog 确认（选中本数+不可恢复+danger），实测弹窗拦截 |
| c) 筛选无结果 | 误指「去导入」，手动逐个清筛选 | 专属文案+「清除筛选」一键复位（实测：空态出现→清除→7 册复位） |
| d) AI 未配置（回想想） | 5 秒 toast，错过无出路 | 弹层内常驻文案+「去配置」直达锚点，动作按钮保留可重试 |
| e) 纯漫画馆藏问藏书 | 提问框照摆，撞误导性报错 | 指路空态+去书库/去导入双出口 |
| f) 桌面任意页检索 | 最深 2 跳 | Ctrl/Cmd+K 1 键直达（实测四场景：裸按跳转/输入聚焦让位/弹层让位/关闭恢复） |
| g) 听书失败 | 控制条假活，暂停键空转 | 失败即复位（实测：onerror→按钮回「开始听书」、徽标消失） |

四原则复查：①方向感——各空态均有下一步指引（补齐 5 处）；②死角与折返——筛选死路（c）、AI 配置折返（d）、问藏书伪错误（e）消除；③步数——b/c/d/e/f 各有量化下降；④终点反馈——批量删除有确认、导入失败有 toast、台账空态有出口。

### 2.2 UX 方案清单（本轮全部实施）

| 编号 | 优先级 | 层级 | 内容 | 收益 |
|---|---|---|---|---|
| UX-001 | P1 | Ⅰ | 特藏室批量条避让底栏 | 手机端三主操作完整可点 |
| UX-002 | P1 | Ⅱ | 特藏室批量删除确认弹窗 | 消除唯一无撤回破坏性动线 |
| UX-003 | P2 | Ⅰ | 书架/摘抄墙筛选空态+一键清除 | 筛选死路折返 → 一次点击 |
| UX-004 | P2 | Ⅱ | 听书失败 ttsActive 复位 | 失败后控制条 3 秒内消失 |
| UX-005 | P2 | Ⅰ | 回想想未配置常驻出路 | 配置往返免重开弹层 |
| UX-006 | P2 | Ⅱ | 原生选文件失败反馈 | 首次导入断点消除 |
| UX-007 | P2 | Ⅰ | 问藏书无可问之书空态 | 伪错误反馈 → 指路 |
| UX-008 | P3 | Ⅰ | 台账行兑现点击+空态出口 | affordance 谎言消除 |
| UX-009 | P3 | Ⅰ | 删设置页馆容量禁用占位 | 假入口消除（连清死代码 33 行） |
| UX-010 | P3 | Ⅱ | 桌面 Ctrl+K 检索 | 任意页 1 键直达 |
| UX-011 | P3 | Ⅰ | Atlas 空态回知识库按钮 | 直链不死路 |
| UX-012 | P3 | Ⅰ | 主书库批量条断点 md→lg | 平板区间三键完整可点（实测 800px） |

**需求缺口未入队列（遗留）**：U-002 批量反选/Shift 多选（Ⅲ 级、移动优先产品触屏用不到，性价比低）。

---

## 阶段三：工程质量测试与诊断

静态审查（技术架构师）：三路并行深读——①基线新代码（听书起播等待+跨章预热）；②服务与数据层 + 第三轮遗留项评估；③页面 UX 走查。动态验证（测试经理）：基线四门禁 + 浏览器 GUI（听书全链路含假引擎事件级探针、12+2 路由冒烟、批量条几何测量、筛选/Ctrl+K/确认弹窗交互实测）。

### 3.1 问题清单与处置（E-001 起本轮编号）

| 编号 | 级别 | 问题（证据） | 处置 |
|---|---|---|---|
| E-001 | P2 | db.ts 无 blocked/blocking/terminated：旧标签页占连接时升级永久挂起零反馈 | ✅ 三回调补齐+toast+让位重连 |
| E-002 | P2 | OPDS 中文凭据 btoa 抛错被吞 → 无鉴权请求 → 误导性 401（`opds.ts:66-74`） | ✅ UTF-8 字节编码+用例 |
| E-003 | P2 | batchMarkAsRead 只改内存，重启回退（`useLibraryStore:864-875`） | ✅ 逐本落库+断言 |
| E-004 | P2 | 恢复备份进度无条件 deleteAll：旧备份静默清空本地进度（`Settings:279`） | ✅ 存在性守卫 |
| E-005 | P3 | 听书起播等待窗口（≤3s 轮询）点暂停是空操作，等待结束无视意图出声（`useSpeech:326` × `TextReader:1098`） | ✅ 意图 ref 贯通+badge 转圈（实测起播后 1ms 落实） |
| E-006 | P3 | 守卫 ref rAF 读实时章索引的亚帧窗口撞切章 → 新章索引配旧章页集；同窗口排队跳页悬空 | ✅ 捕获值+rAF 内补消费 |
| E-007 | P3 | deleteSubLibrary/批量删库无逐项隔离：中途抛错库删书留 UI 卡死 | ✅ 记账口径对齐 batchDelete |
| E-008 | P3 | loadBooks 并发轮孤儿封面 URL 永久泄漏（B 快照看不到 A 新建 URL） | ✅ 模块级在役登记处+并发断言 |
| E-009 | P3 | removeContinueReading 浮动 promise | ✅ 补 catch |
| E-010 | P3 | 特藏室导入批内失败静默吞（`catch{continue}`） | ✅ failedFiles 进 importWarning |
| E-011 | P3 | 同步成功后 localStorage 写失败误报整轮同步失败 | ✅ 独立 try |
| E-012 | P3 | 中文数字裸枚举（一、/（十二））不识别为章节——第三轮遗留 E-018 | ✅ parseChineseNumeral+两形态+闸门复用，4 组新用例 |
| E-013 | P3 | 原生下载无超时：挂起即 busyFile 永久转圈——第三轮遗留 E-021 洞1 | ✅ 120s 超时+缓存清理+成功路径 best-effort |

**核实为干净、无需处理**（新代码审查结论）：预热容器样式/缓存键/LRU 与主管线逐项一致；轮询清理与重入正确；双等待信号时点闭环；与 9047c9d 三修复无新竞态；预热 effect 生命周期干净。**降级为遗留**：C-4 预热字体门顺序（与主管线对称的既有暴露，两侧一致非回归）。

### 3.2 维持遗留（未实施，含第三轮继承）

- ETag 乐观并发（R3 E-019）：需 weak-etag 降级前提，单用户场景窗口小。
- 备份导出含 books/tags/subLibraries 死重（R3 E-022 后半）：产品语义需先裁定（恢复是否含书架；导出不含书文件）。
- R1 继承：TextReader 大文件拆分、React.memo=0、书内检索主线程扫描、双 HTTP/压缩栈、云同步自动触发、Auth/Reader/KnowledgeHub 页面级测试。
- 本轮新增遗留：C-4 字体门对称暴露；U-002 批量反选/Shift 多选。
- 观察项：原生引擎起播看门狗对「speak 永不回调」真机兜底（需真机）；听书通知栏（并行工作流，未入库）。

---

## 阶段四：统一实施

基线 `3abad9d` → 分支 `optimize-20260921`，26 个 commit（25 项队列 + 1 个回归补测），22 文件 +601/-113。队列排序按任务规则（P0→P3；同级先 E 后 UX；UX 按 Ⅰ→Ⅱ→Ⅲ；E 按影响面小→大）。全部单项独立提交、自测、状态更新，无失败无回滚。

| # | 编号 | commit | 验证方式 |
|---|---|---|---|
| 1 | UX-001 | 1368c6d | 实测几何（y663+77 < 781） |
| 2 | UX-002 | 265d794 | 实测弹窗拦截、书未删 |
| 3 | E-001 | 9e5e8e7 | typecheck+storage 31 例+应用启动实测 |
| 4 | E-002 | dadc85b | 新增中文凭据用例 |
| 5 | E-003 | 2c9d2fc | 新增落库断言 |
| 6 | E-004 | 2babd56 | typecheck+守卫语义对照 |
| 7 | UX-003 | 18a1733 | 实测双侧（书架+摘抄墙） |
| 8 | UX-005 | 51400c2 | 新组件用例 |
| 9 | UX-007 | 305f8c0 | 新空态用例 |
| 10 | UX-004 | da52edb | 实测（假引擎 onerror→复位） |
| 11 | UX-006 | f55e2d2 | 原生路径逻辑审（浏览器不可达） |
| 12 | E-005 | 56bb73d | 实测（等待窗口暂停→起播 1ms 落实） |
| 13 | E-006 | 663ff59 | 代码审+分页链回归 |
| 14 | E-009 | 91af2f5 | store 28 例 |
| 15 | E-011 | 9095e12 | typecheck |
| 16 | E-010 | 8e09328 | store 27 例 |
| 17 | E-007 | 6a8a17e | store 27 例（记账口径对齐） |
| 18 | E-008 | f2c9e03 | 新增并发回收断言 |
| 19 | E-013 | 23fc9d8 | typecheck+逻辑审（原生路径） |
| 20 | E-012 | 770bbb9 | 4 组新用例，解析 51 例全绿 |
| 21 | UX-008 | cbedf7d | typecheck+冒烟 |
| 22 | UX-009 | e281ae9 | typecheck+Settings 冒烟 |
| 23 | UX-011 | a14c333 | typecheck+Atlas 冒烟 |
| 24 | UX-012 | 8e31b9f | 实测 800px 几何 |
| 25 | UX-010 | e9b0af7 | 实测四场景 |
| 26 | 回归补测 | c4b4be2 | useSpeech 28 例全绿 |

---

## 阶段五：全量回归验证与交付

### 5.1 回归结论

| 门禁 | 结果 | 说明 |
|---|---|---|
| 自动化测试 | **943/943 全绿**（113 文件） | 基线 918 → 本轮 +11（含外部工作流 +14 中 3 例与轮次重叠口径差）；本轮新增用例：OPDS 中文凭据/标已读落库/并发 URL 回收/中文数字×4/RecapSheet 出路/Ask 空态/听书暂停×2 |
| ESLint + 令牌守卫 | 0 错 | `npm run lint` |
| TypeScript | 0 错 | 全量（外部 ttsNotification 错误已由并行工作流自行修复） |
| 构建 | 通过 | `npm run build`（chunk 体积警告为存量） |
| 全站冒烟 | 14 路由全过（实测） | 主入口/核心功能/关键路径无错误文本 |
| 听书全链路（实测） | 起播锚定（51ms）/暂停/停止/失败复位/等待窗口暂停贯通/章末续播（1→2→3 章连续） | 暂停恢复续播由新增单测定论（浏览器疑似问题为测试书单块章节道具效应） |

**问题关闭率：25/25 = 100%**（含两项第三轮遗留 E-018/E-021洞1 关闭）。

### 5.2 执行摘要

- 三维度问题分布：本轮识别 P1×2 / P2×9 / P3×14（含遗留项处置），共 25 项入队。
- 实施完成率 100%（25/25），失败 0、回滚 0、冲突搁置 0。
- 动线改善量化：六条动线步骤/可达性改善（见 2.1），其中手机端特藏室批量三键从「被遮挡误触」到完整可点、桌面检索从 2 跳到 1 键。
- 遗留项 TOP3：①备份导出/恢复不对称与事务化（产品语义先裁定）；②TextReader 大文件拆分（R1 继承）；③云同步 ETag 乐观并发（需 weak-etag 降级设计）。

### 5.3 回滚指引

- 基线标识：`comics` @ `3abad9d`。
- 整体回滚：`git checkout comics && git branch -D optimize-20260921`（或对 comics 执行 `git reset --hard 3abad9d` 前先确认其上无其他合入）。
- 单项回滚：每项独立 commit，`git revert <hash>` 即可（队列表含全部 hash，无相互依赖）。
- 注意：工作区尚有并行工作流的未提交改动（听书通知栏），回滚操作不会触碰它们；合并本分支时建议外部工作流先自行提交，避免同文件（TextReader）交叉。
